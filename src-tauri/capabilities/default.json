{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the pet and settings windows",
  "windows": ["pet", "settings"],
  "permissions": [
    "core:default",
    "core:window:allow-set-position",
    "opener:default",
    "store:default"
  ]
}
